"use client";

import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("click")}>AddToCart</button>
    </div>
  );
};

export default AddToCart;
